{
  "name": "truffl-website",
  "version": "1.0.0",
  "private": true,
  "description": "Truffl marketing website \u2014 static site + Vercel serverless API stubs",
  "scripts": {
    "build": "node build.js",
    "check": "node build.js --check",
    "prestart": "node build.js",
    "start": "node server.js",
    "dev": "node build.js && node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
